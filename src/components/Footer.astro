<footer class="footer">
  <div class="footer-pattern"></div>
  <div class="container footer-grid">
    <div>
      <h3 class="footer-logo">
        <span class="ornament">&#10086;</span>
        KEMI<span class="logo-accent">AI</span>
      </h3>
      <p class="footer-desc">
        Where African ingenuity meets AI innovation. We bridge tradition and technology to build the future.
      </p>
    </div>
    <div>
      <h4 class="footer-heading">Navigate</h4>
      <ul class="footer-links">
        <li><a href="/">Home</a></li>
        <li><a href="/about">Who We Are</a></li>
        <li><a href="/services">What We Do</a></li>
        <li><a href="/contact">Talk to Us</a></li>
      </ul>
    </div>
    <div>
      <h4 class="footer-heading">Expertise</h4>
      <ul class="footer-links">
        <li><a href="/services">Digitization</a></li>
        <li><a href="/services">Numerical Transformation</a></li>
        <li><a href="/services">AI Transitioning</a></li>
        <li><a href="/services">Consulting</a></li>
      </ul>
    </div>
    <div>
      <h4 class="footer-heading">Connect</h4>
      <ul class="footer-links">
        <li><a href="mailto:hello@kemiai.dev">hello@kemiai.dev</a></li>
        <li><a href="/contact">Send a message</a></li>
      </ul>
    </div>
  </div>
  <div class="container footer-bottom">
    <p>&copy; {new Date().getFullYear()} KEMI AI. Crafted with purpose.</p>
  </div>
</footer>

<style>
  .footer {
    position: relative;
    background: var(--color-dark);
    color: var(--color-brown-light);
    padding: 80px 0 0;
    overflow: hidden;
  }

  .footer-pattern {
    position: absolute;
    inset: 0;
    opacity: 0.03;
    background-image:
      radial-gradient(circle at 20% 80%, var(--color-gold) 1px, transparent 1px),
      radial-gradient(circle at 80% 20%, var(--color-gold) 1px, transparent 1px),
      radial-gradient(circle at 40% 40%, var(--color-gold) 1px, transparent 1px),
      radial-gradient(circle at 60% 60%, var(--color-gold) 1px, transparent 1px);
    background-size: 60px 60px;
    pointer-events: none;
  }

  .footer-grid {
    position: relative;
    display: grid;
    grid-template-columns: 1.5fr 1fr 1fr 1fr;
    gap: 48px;
    padding-bottom: 48px;
  }

  .footer-logo {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--color-cream);
    margin-bottom: 12px;
  }

  .ornament {
    color: var(--color-gold);
    font-size: 1rem;
  }

  .logo-accent {
    color: var(--color-gold);
    font-family: 'Playfair Display', serif;
    font-style: italic;
  }

  .footer-desc {
    font-size: 0.9rem;
    line-height: 1.7;
    max-width: 300px;
    color: var(--color-brown-light);
  }

  .footer-heading {
    color: var(--color-gold-light);
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 20px;
  }

  .footer-links {
    list-style: none;
  }

  .footer-links li {
    margin-bottom: 10px;
  }

  .footer-links a {
    color: var(--color-brown-light);
    font-size: 0.9rem;
    transition: color 0.2s;
  }

  .footer-links a:hover {
    color: var(--color-gold);
  }

  .footer-bottom {
    position: relative;
    border-top: 1px solid rgba(212, 160, 23, 0.12);
    padding: 24px 0;
    font-size: 0.85rem;
    color: var(--color-brown-light);
  }

  @media (max-width: 768px) {
    .footer-grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }
  }
</style>
